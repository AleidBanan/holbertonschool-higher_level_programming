# SQL – More Queries

This project focuses on advanced SQL concepts such as user privileges, constraints, joins, subqueries, and relationships between tables.

Each task is written as a separate `.sql` file and executed using the MySQL command-line interface.

---

## Requirements

- Ubuntu 20.04 or 22.04
- MySQL 8.0+
- All SQL files:
  - End with a new line
  - Use valid SQL syntax
  - Use `--` for comments when needed
  - Are executed using `mysql` CLI

---

## How to Run

### Start MySQL
```bash
service mysql start
